{
    "$schema": "https://opencode.ai/config.json",
    "provider": {
        "ghe": {
            "options": {
                "enterpriseUrl": "{env:OPENCODE_GHE_URL}"
            }
        },
        "github-copilot": {
            "options": {
                "enterpriseUrl": "{env:OPENCODE_GHE_URL}"
            }
        }
    }
}
